import React from "react";
import styled from "styled-components";

const Help = props => {
  const HelpContainer = styled.div`
    height: 83.75vh;
    border: 1px solid black;
  `;

  return <HelpContainer>Help</HelpContainer>;
};

export default Help;
